spring:
  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml
  h2:
    console.enabled: false
  datasource:
    url: jdbc:h2:mem:test
    username: test
    password:
  jpa:
    hibernate:
      ddl-auto: none